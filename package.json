{
  "name": "ezorder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "live": "npm run watch & npm run bs",
    "watch": "watchify src/app.js -o dist/bundle.js -v --debug",
    "build": "browserify src/app.js -o dist/bundle.js -v",
    "bs": "browser-sync start --config bs-config.js",
    "install-tools": "npm i watchify -g & npm i browser-sync -g",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Clement R",
  "license": "ISC"
}
